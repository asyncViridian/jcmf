q_chr*_newParams_5block-10to3000-all
	Settings in settings/newParams_5block-10to3000-all
        Block-based merging (5 blocks)
        Length threshold is from 10 to 3000
        Includes all species
        Used CMfinder parameters:
                maxCandPerSeq 60
                fracSeqWMotif 0.70
                maxNumSingleS 20
                maxNumDoubleS 20
                minSpanSingle 20
                maxSpanSingle 150
                minSpanDouble 20
                maxSpanDouble 150
q_chr*_emuBigscan_5block-10to3000-all
	Settings in settings/emuBigscan_5block-10to3000-all
        Block-based merging (5 blocks)
        Length threshold is from 10 to 3000
        Includes all species
        Used CMfinder parameters:
                maxCandPerSeq 40
                fracSeqWMotif 0.80
                maxNumSingleS 5
                maxNumDoubleS 5
                minSpanSingle 30
                maxSpanSingle 100
                minSpanDouble 40
                maxSpanDouble 100
q_chr*_10block-10to3000-all
	Settings in settings/emuBigscan_10block-10to3000-all
        Block-based merging (10 blocks)
        Length threshold is from 10 to 3000
        Includes all species
        Used CMfinder parameters:
                maxCandPerSeq 40
                fracSeqWMotif 0.80
                maxNumSingleS 5
                maxNumDoubleS 5
                minSpanSingle 30
                maxSpanSingle 100
                minSpanDouble 40
                maxSpanDouble 100

